apiVersion: v1
kind: Service

metadata:
  name: churn-frontend-service
  namespace: churn-app

spec:
  type: NodePort

  selector:
    app: churn-frontend

  ports:
  - port: 5173
    targetPort: 5173
    nodePort: 30128