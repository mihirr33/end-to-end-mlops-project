apiVersion: v1
kind: Service

metadata:
  name: churn-frontend-service
  namespace: churn-app

spec:
  selector:
    app: churn-frontend

  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173

  type: NodePort